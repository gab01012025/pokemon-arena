{
  "buildCommand": "prisma generate && prisma db push && next build",
  "framework": "nextjs",
  "regions": ["gru1"],
  "env": {
    "NEXT_PUBLIC_GAME_SERVER_URL": "https://pokemon-arena-ws.railway.app"
  }
}
